# Init Templates

**Status:** Placeholder — init UX design not yet complete.

Templates in this directory will define the conversational onboarding flow: question sets, seed schema handling, profile generation, and assistant introduction.

When init is fully designed, this directory will hold:
- `init-prompt.md` — the system prompt template for init mode
- `init-flow.yaml` — question sequence and branching logic
- `seed-config.yaml` — how seed data maps to profile fields

See `spec/adrs/lmf-adr-016-initiation-mode.md` and `spec/adrs/lmf-adr-008-two-layer-onboarding.md` for the current design state.
